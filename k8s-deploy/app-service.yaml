apiVersion: v1
kind: Service
metadata:
  labels:
    app: app-svc1
  name: app-svc1
  namespace: app-space
spec:
  type: NodePort
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: app-svc1